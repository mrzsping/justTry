1.性能：浏览器端（图像缓存、压缩、合理布局、减少cookie传输、CDN、反向代理） -> 应用服务器（缓存、异步、集群）-> 代码（多线程，改善内存管理）-> 数据服务器（索引、缓存、sql）-> 指标 （响应时间 + tps（每秒事务处理量）+ 系统性能计数器）

2.可用：冗余 + 集群

3.伸缩：集群中添加新服务器（提供无差别服务 + 容易 + 数量限制） -> 改进缓存路由算法保证缓存可访问 -> 关系数据库（主从热备不适合大规模，路由分区）

4.拓展：新增业务，对产品无影响 -> 少耦合 -> 事件驱动（消息队列） + 分布式服务（业务与可复用服务分离）

5.安全：