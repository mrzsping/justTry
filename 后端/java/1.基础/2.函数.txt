1.结构: 修饰符  返回值类型(没有时则为void)  名称  参数

2.调用: 对象调用

3.实参数目、类型、次序与形参匹配

4.重载: 名称相同实际不同(形参类型、个数、顺序)，参数名称不同不构成重载

5.递归: 自己调用自己，有一个结束条件，缺点：占用大量的系统堆栈，内存消耗巨大，层次多时比循环慢，避免使用

6.重写: 子类继承父类，改变父类方法，子类返回的应包括在父类以内，不能是父类的父类

